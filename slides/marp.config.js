module.exports = {
  // Basic Marp configuration
  inputDir: './',
  output: './dist',
  
  // Set the theme and output formats
  themeSet: './theme.css',
  html: true,
  pdf: true,
  
  // Ready.net brand colors can be defined in theme.css
  allowLocalFiles: true,
  
  // Enable MathJax and other extensions if needed
  math: false,
  
  // Disable the progress bar
  progress: false
};
